# The exporter package intentionally does not depend on pgBackRest. Install and
# configure pgBackRest separately on hosts whose backup metrics should be exposed.
PGBACKREST_EXPORTER_OPTS='--log.level=info --backrest.config=/etc/pgbackrest/pgbackrest.conf'
